# Archivo de configuración de ejemplo — NO contiene credenciales reales.
# Copiar como config.local y completar con valores reales (fuera del control de versiones).

SUPABASE_URL=https://proyecto.supabase.co
SUPABASE_ANON_KEY=clave-anonima

PAYPHONE_CLIENT_ID=client-id
PAYPHONE_CLIENT_SECRET=client-secret

APP_ENV=development